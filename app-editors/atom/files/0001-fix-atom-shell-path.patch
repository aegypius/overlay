diff --git a/atom.sh b/atom.sh
index b4e06a7..139383b 100755
--- a/atom.sh
+++ b/atom.sh
@@ -68,7 +68,7 @@ if [ $OS == 'Mac' ]; then
 elif [ $OS == 'Linux' ]; then
   SCRIPT=`readlink -f "$0"`
   USR_DIRECTORY=`readlink -f $(dirname $SCRIPT)/..`
-  ATOM_PATH="$USR_DIRECTORY/share/atom/atom"
+  ATOM_PATH="$USR_DIRECTORY/share/atom-shell/atom"
 
   [ -x "$ATOM_PATH" ] || ATOM_PATH='/tmp/atom-build/Atom/atom'
 
