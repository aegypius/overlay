diff --git a/bin/default b/bin/default
index 285d80a..4e3d7c6 100644
--- a/bin/default
+++ b/bin/default
@@ -6,7 +6,7 @@ REPO_PATH="$SRC_DIR/$REPOSITORY"
 DOCKERFILE="$REPO_PATH/Dockerfile"
 
 PUBLIC_IP=$(curl -s http://ipv4.icanhazip.com)
-PRIVATE_IP=$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4)
+PRIVATE_IP=$(ifconfig | sed -En 's/127.0.0.1//;s/.*inet (addr:)?(([0-9]*\.){3}[0-9]*).*/\2/p' | head -n1)
 
 DOMAIN_SUFFIX="$PUBLIC_IP.xip.io"
 XIP_IO="$PUBLIC_IP.xip.io"
